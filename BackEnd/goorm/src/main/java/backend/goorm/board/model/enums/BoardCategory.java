package backend.goorm.board.model.enums;

/**
 * 게시글 카테고리
 * NONE -> 카테고리 없음
 * WORKOUT -> 운동
 * FOOD -> 맛집
 * AD -> 광고
 * CONCERN -> 고민
 * HOBBY -> 취미
 * NEIGHBOR -> 동네
 * ETC -> 기타
 */
public enum BoardCategory {
    NONE,
    WORKOUT,
    FOOD,
    AD,
    CONCERN,
    HOBBY,
    NEIGHBOR,
    ETC
}
